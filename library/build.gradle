apply plugin: 'com.android.library'

group = 'com.github.mklimek'

android {
    compileSdkVersion 24
    buildToolsVersion '24'

    defaultConfig {
        minSdkVersion 10
        targetSdkVersion 24
        versionCode 1
        versionName "1.0.0"
    }
}

